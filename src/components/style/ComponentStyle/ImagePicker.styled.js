import styled from "styled-components";

export const ImagePickerComponent = styled.div`
  .vertical_line {
    border-Right: 3px solid ${({ theme }) => theme.color.light_grey};
    height: 95%;
  }

`;
